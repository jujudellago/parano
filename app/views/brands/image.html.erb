<h2><%= "Brand Image for:".t %>  <%= @brand.name %></h2>
<div id="pages_editor_container">
<div class="contentText">
		<fieldset>	<legend><%="Image".t %>:</legend>
			<div id="icons_list"  >
	  			<%= render(:partial => 'icon', :locals => { :icon => @icon,:no_icon=>false}) %>
			</div>
		</fieldset>
<%= submit_tag("Finish".t, :class=>'fancy_ok',:onclick=>"window.location.href='#{brands_url}'") %>
</div>
</div>
<%= render(:partial => 'upload_icon_form')%>
<%= upload_frame %>
